# August 2026 Emergency Monthly Governance — 2026-08-25

**Authority:** explicit-human under `monthly-governance` v1.1  
**Source snapshot:** `d7445829eff9037c0965b547aea6ce822c284f92`  
**Accepted Weekly input:** PR #51 / `RUN-20260823T145740000000Z-weekly-evidence-synthesis-6375ac8eb1cac4d6670b`  
**Purpose:** recover work-conserving Daily routing without weakening the STOR falsifier or changing canonical quest priority.

## Durable belief shifts

This emergency transaction consolidates the six accepted August belief events now present on `main`.

- `STOR.constraint_accounting` remains strengthened: mission and host-service costs must be explicit rather than treated as free background context.
- `STOR.fast_rotator_surface_viability` is weakened by the accepted review-corrected falsifier. The result is local to the declared fast-rotator surface grid and does not select hosted operation, larger geometry, or a replacement material assumption.
- `SIM.measurement_boundary` is strengthened: finite-time stale coverage is reproducible, while the identical body-fixed schedule remains non-informative about rotation-regime effects.
- `CALENDAR.fail_closed_validation` and `CALENDAR.deterministic_staging` are strengthened by accepted validation and deterministic staging work.
- `META.wide_angle_steering_abstraction` is mildly strengthened as a modeling abstraction only; the cited result remains numerical, non-SNS-scale, and non-flight evidence.

Raw belief events and evidence remain immutable. `calendar/consolidated_beliefs.json` is a generated governance view, not a replacement for their provenance.

## Reversal / falsifier disposition

Accepted QST-STOR-0002 evidence reports `FALSIFIED_ON_DECLARED_GRID` for the currently parameterized fast-rotator surface escape.

Monthly disposition:

1. Keep `QST-STOR-0002` active at P0.
2. Locally block only the fast-rotator surface route as presently parameterized.
3. Preserve the falsifier and its review-correction lineage as accepted history.
4. Do not choose hosted operation, larger geometry, revised emissivity, or another assumption-changing architecture in this transaction.
5. A future STOR slice must be separately authorized around an explicit architecture or bounded assumption decision.

A local route stop is not a program-wide research freeze.

## Quest accounting and work conservation

All eight active quests remain active. No queue membership or priority label changes.

`QST-ARCI-0001` is already active at P0 and already lies inside the August delegation envelope. Monthly therefore enacts it as the **current executable spawn** while the STOR fast-rotator route is locally blocked.

This is execution-slot routing, not queue reprioritization. Pre-Game may issue one bounded `refine_existing` authorization for the existing ARCI synthetic-target / evidence-type / missing-data / weight-confidence-sensitivity slice if source, delegation, ownership, and write surfaces remain clean.

If a blocker is demonstrated to be program-global, shared-dependency, or protected-human in scope, Pre-Game must still stop and escalate rather than route around it.

## PR disposition

- PR #51 is accepted and merged on canonical main at `d7445829eff9037c0965b547aea6ce822c284f92`; its main backstop passed tests, automation-transaction, and baseline-artifacts.
- PR #50 is closed unmerged as superseded stale work. Its duplicate-source and missing-`state_hash` failures remain historical evidence and were not rewritten or imported.

## Canonical memory

`memory/mem_log_short.md` is refreshed because its prior one-shot move named the already-completed matched mission-dependency ledger. The current one-shot move is now the bounded ARCI synthetic-target sensitivity slice, with the STOR route decision kept separate.

## Delegation

Carry forward `DELEG-20260807-HUMAN-FAST-TRIAGE-V1` unchanged through its existing expiry `2026-09-01T14:00:00Z`.

No contract, schema, protected surface, external-publication authority, or delegation scope is widened by this transaction.

## Graph-routing follow-up

The repeated August stall exposed a control-flow defect: Pre-Game treated a quest-local L2 stop as if it were a global stop. This monthly transaction fixes the immediate state only.

A separate reviewed Graph Routing v1.1 contract transaction should encode:

- local stop → local escalation + deterministic alternate eligible route;
- slot routing ≠ queue reprioritization;
- continue a live valid owner first;
- otherwise choose the highest-priority eligible active quest, with stable active-index tie-breaking;
- global/shared/protected blocker → global escalation and no authorization;
- regression coverage proving that one locally blocked P0 quest does not freeze another delegated independent P0 quest.

## Final-week focus

1. Resume one bounded Daily research artifact through `QST-ARCI-0001`.
2. Review and qualify Graph Routing v1.1 without widening Daily's authority.
3. Keep the next STOR architecture experiment behind an explicit scientific/governance decision rather than improvising around the accepted falsifier.
