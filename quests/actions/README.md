# Semantic Quest Actions

Store immutable action records using `refine_existing`, `propose_new`, `block`, `retire`, `merge_with`, or `no_action`. Weekly evidence proposes; monthly governance enacts queue-wide changes.
