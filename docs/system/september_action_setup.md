# September action setup — Issue #79

Human approved September's expansion from individually authorized refinements to complete bounded research cycles. AGENTS.md is the full instruction entrypoint; versioned active contracts provide role detail. Scheduler prompts are generated bootloaders.

- Monthly: external literature, strategy, beliefs and standing policy.
- Weekly: delivery, four sequential slices, complete backlog review, delegated quest lifecycle and coming-week direction.
- Pre-Game: two bounded maintenance/delivery slices and qualified merges; no scientific source implementation.
- Daily: one coherent research slice under standing authority, through qualification and permitted merge.
- Observatory: accepted-source site projection under its separate repository instructions.

The action validator constrains declared role/scope/path/budget/expiry; semantic review certifies neither scientific truth nor authority by itself. Retain exact-head qualification, one owner, negative results, fixed evaluator/holdout discipline, historical replay and explicit human boundaries. Full Git history is used to independently bind replay contexts and delegated authority bytes to accepted source commits.

## September research entry

QST-SYNTH-0001 occupies completed ARCI's released slot. First gate: a reviewed tiny three-arm thermal protocol and non-generating validation harness. Actual generator/evaluator interface discovery precedes implementation choices. Campaign generation and holdouts await the accepted protocol; no results are claimed by activation. SIM3's fixed sweep remains independent fallback work.

All 13 prior QA records and the two open issues at source have dispositions in quests/dispositions/2026-09-06.json. Those decisions preserve raw proposals and prior results. Weekly must refresh the inventory from live GitHub and current QA files.

## Scheduling

Normal five active tasks: Aurora Daily Vector; SNS Pre-Game Triage; SNS Daily Research Operator; SNS Weekly Research Delivery; SNS Daily Observatory Refresh. Monthly stays configured and paused between reviews. Preserve task identities and cadences. September 20 reviews expanded authority. September 30 reviews/renews expiring standing scope; swap Observatory's slot to Monthly for the governance review, then restore Observatory after accepted disposition. A calendar event is a gate/reminder, not an automatic scheduler toggle.

Existing task contexts are retained. Available update tooling has no connector-attachment field; naming @GitHub, @Google Drive, @Google Calendar and optional @Wolfram in the prompt is not proof of attachment. Verify harmless connected operations before configuration and verify actual run behavior separately. Wolfram's server returned 404 during setup; optional computation cannot block standard-library provenance.

## Provenance fallback

Every accepted main push produces a Weekly base snapshot automatically. For another loop, dispatch `provenance-snapshot.yml` on accepted main with exact expected main SHA and loop ID. It resolves contracts and record identities from that commit, fetches live open PRs, rechecks main, and exposes snapshot.json and generated IDs in logs/artifact. It creates no research receipt and consumes no experiment. An agent must add any extra source dependencies required by its specific decision and recompute through the same canonical implementation; the base snapshot is not a claim that all possible inputs were read.

## Review measurements

Accepted experimental artifacts; evidence-backed quest dispositions; waiting time; human interventions. Administrative closures are reported separately. No quota encourages premature scientific completion. The September 20 review may narrow a scope when observed failures justify it; independent safe work should remain executable.
