# License Notes

The candidate contains newly authored, standalone utility code, tests, documentation, and tiny synthetic fixtures. No third-party code is vendored. Runtime behavior uses only the Python standard library; tests use pytest.

The candidate excludes private datasets, model weights, saved training artifacts, repository history, and private research documentation. Before public import, attach the intended public-project license and rerun the privacy and qualification commands on the exact copied bytes.

Current human release state: `NOT_REVIEWED`.
